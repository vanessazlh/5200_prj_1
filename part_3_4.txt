SELECT DISTINCT
    r.email AS reviewer_email
FROM Reviews r
JOIN Paper p ON r.pid = p.pid
WHERE
    p.avg_score > 0 -- Add this to prevent division-by-zero errors
    AND
    ABS((r.originality + r.correctness + r.fitness) / 3.0 - p.avg_score) / p.avg_score > 0.33;
